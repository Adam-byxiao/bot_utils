# KWS计算工具依赖包

# 核心依赖
paramiko>=2.7.0          # SSH连接库
cryptography>=3.0.0      # paramiko的加密依赖

# 数据处理
pandas>=1.3.0            # 数据分析（可选）
numpy>=1.20.0            # 数值计算（可选）

# 日志和配置
colorlog>=6.0.0          # 彩色日志输出（可选）

# 开发和测试依赖（可选）
pytest>=6.0.0            # 单元测试
pytest-cov>=2.0.0        # 测试覆盖率
black>=21.0.0             # 代码格式化
flake8>=3.8.0             # 代码检查